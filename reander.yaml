services:
  - type: web
    name: exam-app
    env: node
    region: oregon
    plan: free
    buildCommand: cd backend && npm install
    startCommand: node backend/server.js
    autoDeploy: true
